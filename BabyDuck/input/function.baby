program function;
var x: float;

void print_sum(num1: int, num2: int) [
    var a, b, c, d: int;
    {
        print(num1 + num2);
    }
];

void print_sum_2(num: int) [
    var a, b, c, d: float;
    {
        b = 2;
        c = 4.3;
        a = 10;
        a = b * a + c;
        print(num + a);
    }
];

main {
    x = 3.7;
    print_sum(x, 4.2);
    print_sum_2(7.5);
}

end

