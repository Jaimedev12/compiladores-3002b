program function;
var x: int;

void print_sum(num1: int, num2: float) [
    var a, b, c, d: int;
    {
        print(num1 + num2);
    }
];

void print_sum_2(num1: int, num2: int) [
    var a, b, c, d: float;
    {
        a = b * a + c;
        print(num1 + a);
    }
];

main {
    x = 3;
    print_sum(x, 4.2);
}

end

